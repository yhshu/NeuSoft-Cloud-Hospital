<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//MyBatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">

<configuration>
    <!-- 引入外部配置文件 -->
    <properties resource="db.properties"/>
    <settings>
        <setting name="logImpl" value="LOG4J"/>
    </settings>

    <!-- 为 Java Bean 类 起别名 -->
    <typeAliases>
        <!-- 别名方式1：逐个配置，type 字段是类的全路径，alias 字段是类别名 -->
        <!-- <typeAlias type="com.neusoft.medical.bean.Patient" alias="Patient"/>-->
        <!-- 别名方式2：自动扫描，将 Java 类的名称作为类的别名 -->
        <package name="com.neusoft.medical.bean"/>
    </typeAliases>

    <!-- 配置 MyBatis 运行环境 -->
    <environments default="mysql">
        <environment id="mysql">
            <!-- type="JDBC" 代表使用 JDBC 的提交和回滚来管理事务 -->
            <transactionManager type="JDBC"/>

            <!-- MyBatis 提供3种数据源类型，分别是：POOLED, UNPOOLED, JNDI -->
            <!-- POOLED: 支持 JDBC 数据源连接池 -->
            <!-- UNPOOLED: 不支持数据源连接池 -->
            <!-- JNDI: 支持外部数据源连接池 -->
            <dataSource type="POOLED">
                <!-- 从外部配置文件读取数据库连接池配置 -->
                <property name="driver" value="${driver}"/>
                <property name="url" value="${url}"/>
                <property name="username" value="${username}"/>
                <property name="password" value="${password}"/>
            </dataSource>
        </environment>
    </environments>

    <!-- mybatis.cfg.xml 里注册 UserMapper.xml 文件-->
    <mappers>
        <!-- 告知映射文件方式1：逐个配置 -->
        <mapper resource="mapper/Patient.xml"/>
        <!-- <mapper class="com.neusoft.medical.dao.PatientDao"/>-->

        <!-- 告知映射文件方式2：自动扫描包内的 Mapper 接口与配置文件 -->
        <!-- <package name="com/neusoft/medical/dao"/> -->
    </mappers>

</configuration>
