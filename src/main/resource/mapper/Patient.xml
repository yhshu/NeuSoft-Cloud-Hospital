<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.neusoft.medical.dao.PatientDao">

    <select id="findAll" resultType="Patient">
        select *
        from hospital.Patient
        order by patientId;
    </select>

    <select id="findById" parameterType="java.lang.String" resultType="Patient">
        select *
        from hospital.Patient
        where patientId = #{patient_id}
    </select>

    <insert id="add" parameterType="Patient">
        insert into Patient(patientId, patientName)
        values (uuid(), #{patientName})
    </insert>

    <update id="update"></update>

    <delete id="deleteById"></delete>

    <delete id="multiDelete" parameterType="Integer">
        delete from hospital.Patient
        <where>
            patientId in
            <foreach collection="ids" item="patientId" open="(" close=")" separator=",">
                #{patientId}
            </foreach>
        </where>
    </delete>
</mapper>
